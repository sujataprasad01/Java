import java.util.Scanner;

public class CaseCheck {
    public static void main(String[] args) {
        Scanner in=new Scanner(System.in);
//        String ch=in.next().trim().charAt(0);
        String ch=in.next().toUpperCase();
        System.out.println(ch);
    }
}
